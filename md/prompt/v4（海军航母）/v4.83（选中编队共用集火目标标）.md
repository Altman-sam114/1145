# v4.83 / 选中编队共用集火目标标

## 版本
v4.83。v4.82 伤害飘字已验收。下一优先级：多选编队集火同一目标时的共用目标标。

## 目标
- 当 >=2 个玩家选中机动单位共享同一存活且玩家已知 `attackTarget` 时，在目标位置显示红色集火标：外环 + 十字 + `FOCUS n`。
- 节点挂在 `effectsLayer` 的 `focusFireMarkerNode`，随选中/HUD 刷新；无共用目标时隐藏。
- 不改攻击分配、伤害、AI、迷雾；单选或目标未知不显示。
- CI air capture 让至少 2 架 Blue 空军共享同一 Red 目标，主截图可验证。
- 同步 README / flow / flowchart / test。

## 非目标
本地测试、XCTest、pbxproj 团队号、改命令语义。
