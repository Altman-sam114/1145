# v4.82 / 命中伤害飘字

## 版本

v4.82。v4.81 选中命令意图线已验收。下一优先级：战斗命中瞬时可读反馈。

## 目标

- `fire(...)` 对玩家可见命中显示短促伤害飘字：`-\(Int(damage))`。
- 显示边界：目标为玩家单位，或敌方目标满足 `isKnownToFaction(..., observer: .player)`；未知敌军不显示。
- 进入 `effectsLayer`，约 0.7s 上浮淡出；不改伤害、射程、目标、AI、迷雾。
- CI air capture 在导弹命中附近显示 persistent 示例飘字。
- 同步 README / flow / test。

## 非目标

- 不新增暴击、护甲公式、音效、本地测试、XCTest、pbxproj 团队号。
